<h1>Editing day</h1>

<%= render 'form' %>
<%= button_to 'Delete', trip_day_path(@trip, @day), method: :delete, class: 'btn btn-danger' %>
<hr>
<%= link_to 'Show', trip_day_path(@trip, @day) %> |
<%= link_to 'Back', trip_path(@trip) %>
