<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, placeholder: "Name your trip." %>
  </div>
  <div class = "field">
    <%= f.label :address %><br>
    <%= f.text_field :address, placeholder: "We recommend using a zip code." %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, placeholder: "(Optional)" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
